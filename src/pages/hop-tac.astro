---
import BaseLayout from '../layouts/BaseLayout.astro';

const partnershipOptions = [
  {
    title: 'Giới thiệu khách hàng',
    subtitle: 'Dành cho tất cả mọi người',
    description: 'Bạn có quen ai đang kinh doanh VLXD? Chỉ cần giới thiệu, chúng tôi lo phần còn lại. Nhận ngay 2 triệu đồng cho mỗi khách hàng thành công.',
    reward: '2.000.000đ',
    rewardLabel: 'cho mỗi khách hàng thành công',
    features: [
      'Không cần là khách hàng vSmartSell',
      'Chỉ cần giới thiệu, vSmartSell chăm sóc',
      'Nhận thưởng ngay khi ký hợp đồng',
      'Không giới hạn số lượng giới thiệu'
    ],
    link: '/gioi-thieu-khach-hang',
    buttonText: 'Tìm hiểu thêm',
    color: 'blue',
    popular: false
  },
  {
    title: 'Cộng tác viên',
    subtitle: 'Kiếm tiền không giới hạn',
    description: 'Trở thành đối tác chính thức của vSmartSell. Nhận hoa hồng 15-30% giá trị hợp đồng và thu nhập từ 300.000đ cho mỗi khách hàng demo.',
    reward: '15-30%',
    rewardLabel: 'hoa hồng trên giá trị hợp đồng',
    features: [
      'Nhận 300.000đ cho mỗi lượt demo',
      'Hoa hồng lên đến 3.000.000đ/khách',
      'Linh hoạt thời gian, làm việc tự do',
      'Kinh doanh ngành VLXD là lợi thế'
    ],
    link: '/cong-tac-vien',
    buttonText: 'Đăng ký ngay',
    color: 'cyan',
    popular: true
  }
];

const stats = [
  { number: '500+', label: 'Doanh nghiệp VLXD đã tin dùng' },
  { number: '2 triệu', label: 'Thưởng cho mỗi khách hàng' },
  { number: '30%', label: 'Hoa hồng tối đa cho CTV' },
  { number: '24/7', label: 'Hỗ trợ đội ngũ đối tác' }
];

const benefits = [
  {
    icon: 'money',
    title: 'Thu nhập không giới hạn',
    description: 'Giới thiệu càng nhiều, thu nhập càng cao. Không có mức trần hoa hồng.'
  },
  {
    icon: 'clock',
    title: 'Linh hoạt thời gian',
    description: 'Làm việc theo lịch của bạn. Không ràng buộc, không áp lực doanh số.'
  },
  {
    icon: 'support',
    title: 'Hỗ trợ chuyên nghiệp',
    description: 'Đội ngũ vSmartSell lo việc tư vấn, demo và chăm sóc khách hàng.'
  },
  {
    icon: 'trust',
    title: 'Sản phẩm uy tín',
    description: '500+ doanh nghiệp đã tin dùng. Giới thiệu sản phẩm tốt, dễ thuyết phục.'
  }
];
---

<BaseLayout
  title="Hợp tác cùng vSmartSell | Kiếm tiền từ giới thiệu khách hàng"
  description="Tham gia chương trình đối tác vSmartSell. Giới thiệu khách hàng nhận 2 triệu đồng hoặc trở thành cộng tác viên nhận hoa hồng 15-30%."
>
  <!-- Hero Section -->
  <section class="py-20 lg:py-28 bg-gradient-to-br from-slate-900 via-blue-900 to-slate-900 relative overflow-hidden">
    <div class="absolute inset-0 bg-[linear-gradient(rgba(255,255,255,0.03)_1px,transparent_1px),linear-gradient(90deg,rgba(255,255,255,0.03)_1px,transparent_1px)] bg-[size:60px_60px]"></div>
    <div class="absolute top-20 left-20 w-72 h-72 bg-blue-500/20 rounded-full blur-3xl"></div>
    <div class="absolute bottom-20 right-20 w-72 h-72 bg-cyan-500/20 rounded-full blur-3xl"></div>

    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 relative z-10">
      <div class="text-center max-w-4xl mx-auto">
        <div class="inline-flex items-center gap-2 px-4 py-2 bg-white/10 backdrop-blur rounded-full text-white/80 text-sm mb-8">
          <span class="w-2 h-2 bg-green-400 rounded-full animate-pulse"></span>
          Chương trình đang mở đăng ký
        </div>
        <h1 class="text-4xl md:text-5xl lg:text-6xl font-bold text-white mb-6 leading-tight">
          Kiếm tiền cùng
          <span class="bg-gradient-to-r from-cyan-400 to-blue-400 bg-clip-text text-transparent leading-relaxed"> vSmartSell</span>
        </h1>
        <p class="text-xl text-slate-300 mb-12 max-w-2xl mx-auto">
          Bạn có mối quan hệ trong ngành VLXD? Hãy biến mối quan hệ thành thu nhập. Nhận ngay <strong class="text-white">2 triệu đồng</strong> cho mỗi khách hàng thành công.
        </p>

        <!-- Stats -->
        <div class="grid grid-cols-2 md:grid-cols-4 gap-4 max-w-4xl mx-auto">
          {stats.map((stat) => (
            <div class="bg-white/10 backdrop-blur rounded-xl p-4 border border-white/10">
              <div class="text-2xl lg:text-3xl font-bold text-white mb-1">{stat.number}</div>
              <div class="text-slate-400 text-sm">{stat.label}</div>
            </div>
          ))}
        </div>
      </div>
    </div>
  </section>

  <!-- Partnership Options -->
  <section class="py-20 bg-white relative -mt-10 z-20">
    <div class="max-w-5xl mx-auto px-4 sm:px-6 lg:px-8">
      <div class="text-center mb-12">
        <span class="inline-block px-4 py-1 bg-blue-100 text-blue-700 rounded-full text-sm font-medium mb-4">Chọn hình thức phù hợp với bạn</span>
        <h2 class="text-3xl md:text-4xl font-bold text-gray-900">
          2 cách để kiếm tiền cùng vSmartSell
        </h2>
      </div>

      <div class="grid md:grid-cols-2 gap-8">
        {partnershipOptions.map((option) => (
          <div class={`relative bg-white rounded-2xl p-8 border-2 ${option.popular ? 'border-cyan-500 shadow-xl shadow-cyan-500/10' : 'border-gray-200'} hover:shadow-xl transition-all duration-300`}>
            {option.popular && (
              <div class="absolute -top-4 left-1/2 -translate-x-1/2">
                <span class="bg-gradient-to-r from-cyan-500 to-blue-500 text-white text-sm font-semibold px-4 py-1 rounded-full">
                  Thu nhập cao nhất
                </span>
              </div>
            )}

            <div class="text-center mb-6">
              <p class="text-sm text-gray-500 mb-2">{option.subtitle}</p>
              <h3 class="text-2xl font-bold text-gray-900 mb-4">{option.title}</h3>
              <div class={`inline-flex items-baseline gap-1 ${option.color === 'cyan' ? 'text-cyan-600' : 'text-blue-600'}`}>
                <span class="text-4xl font-bold">{option.reward}</span>
              </div>
              <p class="text-gray-500 text-sm mt-1">{option.rewardLabel}</p>
            </div>

            <p class="text-gray-600 text-center mb-6">{option.description}</p>

            <ul class="space-y-3 mb-8">
              {option.features.map((feature) => (
                <li class="flex items-start gap-3">
                  <svg class={`w-5 h-5 ${option.color === 'cyan' ? 'text-cyan-500' : 'text-blue-500'} flex-shrink-0 mt-0.5`} fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7" />
                  </svg>
                  <span class="text-gray-600">{feature}</span>
                </li>
              ))}
            </ul>

            <a
              href={option.link}
              class={`block w-full py-4 text-center font-semibold rounded-xl transition-all ${
                option.popular
                  ? 'bg-gradient-to-r from-cyan-500 to-blue-600 text-white hover:shadow-lg hover:shadow-cyan-500/25'
                  : 'bg-gray-100 text-gray-900 hover:bg-gray-200'
              }`}
            >
              {option.buttonText}
            </a>
          </div>
        ))}
      </div>
    </div>
  </section>

  <!-- Benefits Section -->
  <section class="py-20 bg-slate-50">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
      <div class="text-center mb-16">
        <span class="inline-block px-4 py-1 bg-green-100 text-green-700 rounded-full text-sm font-medium mb-4">Tại sao chọn vSmartSell?</span>
        <h2 class="text-3xl md:text-4xl font-bold text-gray-900 mb-4">
          Lợi ích khi trở thành đối tác
        </h2>
        <p class="text-lg text-gray-600 max-w-2xl mx-auto">
          Chúng tôi không chỉ trả hoa hồng cao. Chúng tôi còn hỗ trợ bạn thành công.
        </p>
      </div>

      <div class="grid md:grid-cols-2 lg:grid-cols-4 gap-6">
        {benefits.map((benefit) => (
          <div class="bg-white rounded-2xl p-6 shadow-sm border border-gray-100 hover:shadow-lg hover:-translate-y-1 transition-all duration-300">
            <div class={`w-12 h-12 rounded-xl flex items-center justify-center mb-4 ${
              benefit.icon === 'money' ? 'bg-green-100 text-green-600' :
              benefit.icon === 'clock' ? 'bg-blue-100 text-blue-600' :
              benefit.icon === 'support' ? 'bg-purple-100 text-purple-600' :
              'bg-cyan-100 text-cyan-600'
            }`}>
              {benefit.icon === 'money' && (
                <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 8c-1.657 0-3 .895-3 2s1.343 2 3 2 3 .895 3 2-1.343 2-3 2m0-8c1.11 0 2.08.402 2.599 1M12 8V7m0 1v8m0 0v1m0-1c-1.11 0-2.08-.402-2.599-1M21 12a9 9 0 11-18 0 9 9 0 0118 0z" />
                </svg>
              )}
              {benefit.icon === 'clock' && (
                <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 8v4l3 3m6-3a9 9 0 11-18 0 9 9 0 0118 0z" />
                </svg>
              )}
              {benefit.icon === 'support' && (
                <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M18.364 5.636l-3.536 3.536m0 5.656l3.536 3.536M9.172 9.172L5.636 5.636m3.536 9.192l-3.536 3.536M21 12a9 9 0 11-18 0 9 9 0 0118 0zm-5 0a4 4 0 11-8 0 4 4 0 018 0z" />
                </svg>
              )}
              {benefit.icon === 'trust' && (
                <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12l2 2 4-4m5.618-4.016A11.955 11.955 0 0112 2.944a11.955 11.955 0 01-8.618 3.04A12.02 12.02 0 003 9c0 5.591 3.824 10.29 9 11.622 5.176-1.332 9-6.03 9-11.622 0-1.042-.133-2.052-.382-3.016z" />
                </svg>
              )}
            </div>
            <h3 class="text-lg font-bold text-gray-900 mb-2">{benefit.title}</h3>
            <p class="text-gray-600">{benefit.description}</p>
          </div>
        ))}
      </div>
    </div>
  </section>

  <!-- CTA Section -->
  <section class="py-20 bg-white">
    <div class="max-w-4xl mx-auto px-4 sm:px-6 lg:px-8">
      <div class="bg-gradient-to-br from-blue-600 to-cyan-500 rounded-3xl p-8 md:p-12 text-center relative overflow-hidden">
        <div class="absolute top-0 right-0 w-64 h-64 bg-white/10 rounded-full blur-3xl"></div>
        <div class="absolute bottom-0 left-0 w-64 h-64 bg-white/10 rounded-full blur-3xl"></div>

        <div class="relative z-10">
          <h2 class="text-3xl md:text-4xl font-bold text-white mb-4">
            Bạn đã sẵn sàng kiếm tiền?
          </h2>
          <p class="text-blue-100 text-lg mb-8 max-w-xl mx-auto">
            Chỉ cần một cuộc giới thiệu đúng người, bạn đã có thể nhận ngay 2 triệu đồng. Bắt đầu ngay hôm nay!
          </p>
          <div class="flex flex-col sm:flex-row gap-4 justify-center">
            <a href="/cong-tac-vien" class="inline-flex items-center justify-center px-8 py-4 bg-white text-blue-600 font-semibold rounded-xl hover:bg-blue-50 transition-colors shadow-lg">
              Đăng ký cộng tác viên
              <svg class="w-5 h-5 ml-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 7l5 5m0 0l-5 5m5-5H6" />
              </svg>
            </a>
            <a href="tel:0866135766" class="inline-flex items-center justify-center px-8 py-4 border-2 border-white/30 text-white font-semibold rounded-xl hover:bg-white/10 transition-colors">
              <svg class="w-5 h-5 mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 5a2 2 0 012-2h3.28a1 1 0 01.948.684l1.498 4.493a1 1 0 01-.502 1.21l-2.257 1.13a11.042 11.042 0 005.516 5.516l1.13-2.257a1 1 0 011.21-.502l4.493 1.498a1 1 0 01.684.949V19a2 2 0 01-2 2h-1C9.716 21 3 14.284 3 6V5z" />
              </svg>
              Gọi ngay: 0866.135.766
            </a>
          </div>
        </div>
      </div>
    </div>
  </section>
</BaseLayout>
